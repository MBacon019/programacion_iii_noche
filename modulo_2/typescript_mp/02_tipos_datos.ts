// Tipos de datos orientados a Control de Vuelos
let numeroVuelo: string = 'AA123';
console.log('Vuelo:', numeroVuelo);

let capacidadPasajeros: number = 180;
console.log('Capacidad:', capacidadPasajeros);

let retrasado: boolean = false;
console.log('Retrasado:', retrasado);

let infoExtra: any = { nota: 'Sin incidencias' };
console.log('Info extra:', infoExtra);

let puertaAsignada: number = 12;
if (puertaAsignada > 0) {
    console.log('Puerta asignada:', puertaAsignada);
} else {
    console.log('Sin puerta asignada');
}
